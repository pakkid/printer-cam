# Connection: upgrade only when the client actually asked for it, otherwise
# keep-alive. Lives in the http context, so it is a separate file.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
